import { Pressable, Text } from 'react-native';

const Button = () => {
  return (
    <Pressable>
      <Text style={{color: "white"}}>Press me</Text>
    </Pressable>
  );
};

export default Button;
